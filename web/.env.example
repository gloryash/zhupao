# 助盲跑 Web 前端环境变量示例
# 复制为 .env.local 后按需修改。请勿在此填写任何密钥 / 口令 / 私有用户数据。

# 公共 CloudBase 环境 ID（可公开）
# 同时被 Web 应用与本地开发代理（server/cloudFunctionProxy.ts）读取。
VITE_CLOUDBASE_ENV=cloud1-d8gbfzr7t6c5dc8bc

# 调用云函数的传输方式（可选，默认按构建模式自动选择）：
#   不设置 = 开发服务器（vite dev/preview）用 proxy，生产构建（vite build）用 sdk
#   proxy = 通过 Vite 开发服务器的 /api/cloud-function 调用本机 tcb CLI（仅本地 QA，部署后不可用）
#   sdk   = 通过浏览器 @cloudbase/js-sdk 匿名鉴权直连 CloudBase（需在控制台配置安全来源 / 匿名登录）
# 生产部署说明：静态站点没有本地代理，必须使用 sdk —— 默认即为 sdk，无需显式设置；
# 如需在本地强制走 sdk 调试，再取消下面一行的注释。
# VITE_CLOUDBASE_TRANSPORT=sdk
